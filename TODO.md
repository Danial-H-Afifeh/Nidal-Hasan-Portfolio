# TODO - GitHub Pages Hosting

- [x] Push local commits to `main` and resolve non-fast-forward error.
- [ ] Implement recommended GitHub Pages deployment using Vite build output.
- [ ] Add/verify `vite.config.ts` for correct base path for GitHub Pages.
- [ ] Add `deploy` script in `package.json` using `gh-pages`.
- [ ] Add `build` output configuration and ensure files referenced by `index.html` work after build.
- [ ] Create/validate `dist/` and commit deployment to GitHub Pages.
- [ ] Provide final GitHub Pages URL and report of steps.

